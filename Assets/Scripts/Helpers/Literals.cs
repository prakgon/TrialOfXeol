namespace Helpers
{
    public static class Literals
    {
        public enum PlayerStates
        {
            Idle,
            Moving,
            Jumping,
            Rolling,
            Attacking
        }

        public enum SliderBarStates
        {
            Idle,
            Processing,
            Returning,
            Cooldown
        }
    }
}